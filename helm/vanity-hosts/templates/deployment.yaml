apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "vanity-hosts.fullname" . }}
  labels:
    {{- include "vanity-hosts.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "vanity-hosts.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "vanity-hosts.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: vanity-hosts
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
